# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node (only if tooling is added later)
node_modules/
npm-debug.log*

# Local env
.env
.env.*

# Local MCP / tooling config (developer-specific)
.mcp.json

# Laravel Boost dev tooling (not part of the skills library)
/vendor/
/composer.json
/composer.lock

# Local artifacts from `npx skills add` (testing installs, not library content)
/.agents/
/skills-lock.json

# Local development notes / roadmap — never pushed
/_dev/
